{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "env": {
    "DATABASE_URL": {
      "description": "PostgreSQL connection string (Vercel Postgres, Supabase, or Neon)",
      "required": true
    },
    "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY": {
      "description": "Clerk publishable key for authentication",
      "required": true
    },
    "CLERK_SECRET_KEY": {
      "description": "Clerk secret key for backend authentication",
      "required": true
    },
    "RAZORPAY_KEY_ID": {
      "description": "Razorpay payment gateway key ID",
      "required": false
    },
    "RAZORPAY_KEY_SECRET": {
      "description": "Razorpay payment gateway secret key",
      "required": false
    },
    "NEXT_PUBLIC_SUPABASE_URL": {
      "description": "Supabase project URL for image storage",
      "required": false
    },
    "NEXT_PUBLIC_SUPABASE_ANON_KEY": {
      "description": "Supabase anonymous key for image uploads",
      "required": false
    },
    "SUPABASE_SERVICE_KEY": {
      "description": "Supabase service role key for backend operations",
      "required": false
    }
  },
  "buildOutputDirectory": ".next"
}
